# Learnings

## https://mitchellh.com/writing/my-ai-adoption-journey

## Linux unplugged --YOLO 668
31:00
open claw and hermes. 
Open claw 
one gateway for many agents, channel based harness
updates done by what is ready
node based 

Hermes
each agent has it's own gateway, each agent can use different model, when making configuration changes only need to restart one agent not all of them called profiles ?dose this each using own configuration or agents will continue with the configuration it was started with until restarted?
closed learning loop; 37:05 
updates done by theme of feature
python based